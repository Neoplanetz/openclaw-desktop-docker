# ============================================================
# OpenClaw Desktop — User Configuration
# ============================================================
# Edit these values and run:  docker compose up -d --build
#
# NOTE: If you change CLAW_USER after the first run,
#       delete the old volume first:
#       docker compose down -v && docker compose up -d --build
# ============================================================

CLAW_USER=claw
CLAW_PASSWORD=claw1234

# OpenClaw npm package version (used at build time: npm install -g openclaw@<version>)
# Examples: latest, 2026.3.28, 2026.2.19
OPENCLAW_VERSION=latest

# OpenClaw browser (CDP Chrome) — set to true to enable
OPENCLAW_BROWSER_ENABLED=false

# Display targeting for OpenClaw Gateway: auto | vnc | rdp | override
#   auto     - follow the most recently active session (VNC ↔ RDP)
#   vnc      - pin to VNC display (:1)
#   rdp      - pin to xRDP display (:10+)
#   override - use OPENCLAW_X_DISPLAY / OPENCLAW_X_AUTHORITY (compose env)
OPENCLAW_DISPLAY_TARGET=auto
